Put exported experiment JSON files here.

Naming convention used by the interpreter:

Example:

Included smoke-test configs:
- `sample_continuous_dot_groups.json` — continuous `rdm-dot-groups` sample (group coherence/speed sampling + cue border).
- `sample_gabor_rewards_milestones.json` — Gabor task with Rewards v2 milestones and summary screens.
- `sample_rdm_continuous_drt.json` — continuous RDM with explicit `detection-response-task-start` / `detection-response-task-stop` bracketing.

## How to run

Use Live Server and open:

- `.../JSON_Interpreter_App/index.html?id=sample_continuous_dot_groups`
- `.../JSON_Interpreter_App/index.html?id=sample_gabor_rewards_milestones&debug=1`
- `.../JSON_Interpreter_App/index.html?id=sample_rdm_continuous_drt&debug=1`
